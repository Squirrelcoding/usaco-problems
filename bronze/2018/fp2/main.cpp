#include <bits/stdc++.h>

using namespace std;

int nxt() {
  int n; cin >> n; return n;
}

#define all(x) begin(x), end(x)

set<int> simulate_path(int starting_point, vector<int> &nums) {
  set<int> result;
  return result;
}


// Finds the minimum number of sets whose union is equal to the entire numbers
int find_min_sets(vector<set<int>> &sets) {
  return 0;
}

int main() {
  int n = nxt();
  vector<int> nums(n);
  generate(all(nums), nxt);
  return 0;
}
